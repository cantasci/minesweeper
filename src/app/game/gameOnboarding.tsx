import React, { FC } from 'react';

import { Typography } from '@mui/material';

const GameOnboarding: FC = () => {
  return <Typography fontSize={30}>Press START to play Minesweeper!</Typography>;
};

export default GameOnboarding;
